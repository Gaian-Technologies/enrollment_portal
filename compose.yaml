services:
  enrollment_portal:
    build:
      context: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      AWS_PROFILE: ${AWS_PROFILE:-}
    ports:
      - "127.0.0.1:8100:8100"
    volumes:
      - ./var:/app/var
      - ${HOME}/.aws:/root/.aws:ro
